<!-- Egyptian Theme Loader -->
<div class="loader-container">
    <div class="pyramid-container">
        <div class="pyramid">
            <div class="pyramid-face front"></div>
            <div class="pyramid-face back"></div>
            <div class="pyramid-face left"></div>
            <div class="pyramid-face right"></div>
            <div class="pyramid-face bottom"></div>
        </div>
        <div class="rotating-circles">
            <div class="circle"></div>
            <div class="circle"></div>
            <div class="circle"></div>
            <div class="circle"></div>
            <div class="hieroglyphic">⚛</div>
            <div class="hieroglyphic">π</div>
            <div class="hieroglyphic">∑</div>
            <div class="hieroglyphic">∞</div>
        </div>
    </div>
    <div class="loading-text">Welcome to EOCS</div>
    <div class="progress-bar">
        <div class="progress"></div>
    </div>
</div>

<!-- Navigation -->
<nav class="main-nav">
    <div class="nav-logo">
        <img src="/assets/ECOS logo - PROFILE COVER-05.svg" alt="EOCS Logo" class="nav-logo-img">
    </div>
    <div class="nav-links">
        <a href="/" class="<%= currentPage === 'home' ? 'active' : '' %>">Home</a>
        <a href="/about" class="<%= currentPage === 'about' ? 'active' : '' %>">About</a>
        <a href="/eligibility" class="<%= currentPage === 'eligibility' ? 'active' : '' %>">Eligibility</a>
        <a href="/competition" class="<%= currentPage === 'competition' ? 'active' : '' %>">Competition</a>
        <a href="/prizes" class="<%= currentPage === 'prizes' ? 'active' : '' %>">Prizes</a>
        <a href="/partners" class="<%= currentPage === 'partners' ? 'active' : '' %>">Partners</a>
        <a href="/contact" class="<%= currentPage === 'contact' ? 'active' : '' %>">Contact</a>
        <a href="/auth/register" class="auth-btn register">Register for Competition</a>
    </div>
    <button class="mobile-menu-btn" title="Toggle Menu" aria-label="Toggle navigation menu">
        <span></span>
        <span></span>
        <span></span>
    </button>
</nav>

<!-- Three.js and required dependencies -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/three@0.128.0/examples/js/loaders/FontLoader.js"></script>
<script src="https://cdn.jsdelivr.net/npm/three@0.128.0/examples/js/geometries/TextGeometry.js"></script>

<!-- Custom loader script -->
<script src="/js/pyramid-loader.js"></script>

<script>
    // Function to show the loader
    function showLoader() {
        const loader = document.querySelector('.loader-container');
        if (loader) {
            loader.style.display = 'flex';
            loader.classList.remove('fade-out');
        }
    }

    // Function to hide the loader
    function hideLoader() {
        const loader = document.querySelector('.loader-container');
        if (loader) {
            loader.classList.add('fade-out');
            setTimeout(() => {
                loader.style.display = 'none';
            }, 800);
        }
    }

    // Show loader immediately
    showLoader();

    // Hide loader after a delay
    setTimeout(() => {
        hideLoader();
    }, 3000);
</script>